export interface DataUpdateModalProps {
  /** Rota atual (hash sem o '#'), para decidir se a tela usa a base atualizada. */
  currentPath?: string;
}

/**
 * Modal de aviso de dados atualizados desativado a pedido do usuário.
 */
export default function DataUpdateModal(_props: DataUpdateModalProps) {
  return null;
}
